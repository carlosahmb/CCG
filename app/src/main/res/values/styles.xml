<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

<style name="RadioButtonTheme">
    <item name="android:colorAccent">@color/colorRadioButtonChecked</item>
</style>
    <style name="dialogSpot" parent="android:Theme.Material.Light.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Material.Medium</item>
        <item name="DialogTitleText">Enviando Mensagem...</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="DialogSpotColor">@color/colorAccent</item>

    </style>

</resources>
